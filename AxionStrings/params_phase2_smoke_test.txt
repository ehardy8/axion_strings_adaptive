# Throwaway smoke test for milestone-2 Phase 2 (composite cross-level
# diagnostics), 2026-09-19: deliberately uses the raw, unrelaxed Fourier IC
# (ic_mode = fourier, not fourier_relaxed) so the run starts in Phase::
# Evolving immediately (no relaxation wait) and its lattice-scale noise
# triggers refinement almost at once (documented Phase-1 finding: a raw
# Fourier IC spuriously pierces nearly every plaquette) -- not physically
# meaningful, but exactly what is needed to exercise a genuinely multi-level
# hierarchy (masks, dx-weighted combination across levels) quickly, to
# sanity-check the composite reduction plumbing itself.

amr.verbose = 1
amr.check_int = -1
amr.plot_int  = -1
amr.max_level = 2
amr.max_grid_size   = 8
amr.blocking_factor = 8
amr.n_error_buf     = 2
amr.regrid_int      = 2

geometry.is_periodic = 1 1 1

evolution.dt_multiplier = 0.2
evolution.max_steps = 30

axion_strings.a_inv = 2.0
axion_strings.c0    = 0.0 # physical

axion_strings.log_mr_over_h_i = 1.0

axion_strings.N  = 128
axion_strings.N1 = 1.0
axion_strings.N2 = 1.0

axion_strings.ic_mode = fourier
axion_strings.k_max_over_mr        = 16
axion_strings.mean_square_variance = 0.5
axion_strings.seed = 42

axion_strings.output_first_log_mr_over_h = 1.0
axion_strings.output_delta_log_mr_over_h = 0.02
